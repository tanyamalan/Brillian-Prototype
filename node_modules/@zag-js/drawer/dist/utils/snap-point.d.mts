import { R as ResolvedSnapPoint, S as SnapPoint } from '../drawer.types-BBkqVjo9.mjs';
import '@zag-js/core';
import '@zag-js/dismissable';
import '@zag-js/types';
import './session.mjs';

interface ResolveSnapPointOptions {
    contentSize: number;
    viewportSize: number;
    rootFontSize: number;
}
declare function resolveSnapPoint(snapPoint: SnapPoint, options: ResolveSnapPointOptions): ResolvedSnapPoint | null;
/**
 * Collapse snap points that resolve to the same height (within 1px), keeping the first occurrence in list order.
 */
declare function dedupeSnapPoints(points: ResolvedSnapPoint[]): ResolvedSnapPoint[];
declare function findClosestSnapPoint(offset: number, snapPoints: ResolvedSnapPoint[]): ResolvedSnapPoint;

export { type ResolveSnapPointOptions, dedupeSnapPoints, findClosestSnapPoint, resolveSnapPoint };
