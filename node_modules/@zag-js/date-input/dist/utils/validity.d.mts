import { n as DateInputSchema, o as IncompleteDate, a as DateSegment, l as SelectionMode } from '../date-input.types-Cd3qf4Va.mjs';
import { Params } from '@zag-js/core';
import '@internationalized/date';
import '@zag-js/date-utils';
import '@zag-js/live-region';
import '@zag-js/types';

declare function getGroupCount(selectionMode: SelectionMode): number;
/**
 * Returns the cumulative segment count for all groups before `index`.
 * Used to convert between global (DOM) and local (per-group) segment indices.
 */
declare function getGroupOffset(allSegments: unknown[][], index: number): number;
/**
 * Resolves the active segment context: all segment groups, the current group,
 * the global-to-local offset, and the local index within the group.
 */
declare function resolveActiveSegment(ctx: Params<DateInputSchema>): {
    allSegments: DateSegment[][];
    segments: DateSegment[];
    offset: number;
    localIndex: number;
};
declare function getActiveSegment(ctx: Params<DateInputSchema>): DateSegment;
declare function advanceToNextSegment(ctx: Params<DateInputSchema>): void;
/** Returns the IncompleteDate for the currently active group. */
declare function getActiveDisplayValue(ctx: Params<DateInputSchema>): IncompleteDate;
/** Replaces the IncompleteDate for the given group index and persists it. */
declare function setDisplayValue(ctx: Params<DateInputSchema>, index: number, dv: IncompleteDate): void;

export { advanceToNextSegment, getActiveDisplayValue, getActiveSegment, getGroupCount, getGroupOffset, resolveActiveSegment, setDisplayValue };
